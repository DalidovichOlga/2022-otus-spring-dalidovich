spring:
  datasource:
    url: jdbc:h2:mem:testbd
  liquibase:
    changeLog: classpath:/db/changelog/changelog.xml
  shell:
    script:
      spring:
        shell:
          script: false
    interactive:
      enabled: false
settings:
  local: ru-RU